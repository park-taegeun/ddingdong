# === 띵동 대시보드 환경변수 (Phase 1) ===
# Vite는 VITE_ 접두사 변수만 클라이언트 번들에 노출.
# 실 파일(.env.local)은 루트 .gitignore로 무시됨 (절대 commit 금지).

# Phase 2 진입 시 서버 API 인증 토큰 (Phase 1 mock 단계에서는 미사용)
VITE_DASHBOARD_TOKEN=

# Phase 2 진입 시 API 베이스 URL (Phase 1 mock 단계에서는 미사용)
VITE_API_BASE_URL=http://localhost:5000
