# 마이크 잡음 근본원인 진단 Runbook (PoC-(45), `env:mic_noiseprobe`)

2026-09-11 신설. 대상 = **ToF 측정 중 조용한 2초 스냅샷의 peak 32767~32768 클립**(PR #53 댓글 「알려진 문제 1」).
이 문서는 **관측 절차와 판정표**만 담는다. 해결책(재배선·필터·I2C 속도 확정·태스크 재배치)은 범위 밖이며,
판정표도 「지지 / 기각」까지만 적는다 — 무엇을 고칠지는 결과를 본 사람이 정한다.

---

## 0. 이 세션이 증명하려는 것

**배경(실측, 2026-09-11 `~/ddingdong-측정결과/2026-09-11/ToF_meta_uplink_runtime.log`)** — 같은 자리·결선·수 분 차이, 펌웨어만 교체:

| 펌웨어 | ToF | 조용한 스냅샷 | peak | rms |
|---|---|---|---|---|
| `mic_uplink` (PR #53) | 측정 ON | 6회 | **32767 ×5, 32768 ×1** | 455~636 |
| `main b3414a4` | 전원만, 측정 OFF | 3회 | 412~464 | 95~96 |

- 32767 / 32768 은 `mic_common.cpp convertMicRawToInt16` saturation 가드의 두 클램프값(`INT16_MAX` / `|INT16_MIN|`)과 **정확히 일치**한다 → 관측값은 "큰 소리"가 아니라 **가드가 잘라낸 raw 이상치**다(코드 인용 + 실측 일치).
- "원인 = ToF 측정 동작"까지는 실측이다. **그 안의 기전은 미분리** — 아래 셋은 전부 **가설(논증)**이다.

| 가설 | 기전 | 근거 성격 |
|---|---|---|
| **H1** I2C 누화 | SCL(GPIO6, 브레드보드 J10)과 마이크 SD(GPIO7, B10)가 **같은 10번 열**(중앙 홈 건너편) + 마이크 M/F 점퍼 공중 부양(6.3(a) / 9.1(d) 결선표) → SCL 엣지가 SD 로 유입 → I2S 비트 오류 → 가드 클램프 | 논증 |
| **H2** 센서 동작 전원 | VL53L5CX 측정(VCSEL 발광) 전류가 공유 3V3 를 흔든다. I2C 트래픽과 무관 | 논증 |
| **H3** 소프트웨어 경합 | tofTask(Core 0, prio 3) ↔ micTask(Core 0, prio 4). 단 `gaps=0` 실측이라 DMA 오버런은 아님 | 논증 |

이 env 는 **한 번에 한 변수만** 바꾸는 모드 5개로 셋을 가른다.

---

## 1. 전제 (전부 지켜야 결과가 비교 가능하다)

- **결선 변경 금지.** 9.1(d) ToF 6가닥 + 6.3(a) 마이크 6가닥 그대로. 재배선은 해결책이므로 이 세션에서 하지 않는다 — 배선을 건드리면 A/B 의 "같은 결선" 전제가 깨져 m0 대조군이 무효가 된다.
- **조용한 환경.** 사람 대화 없음. 에어컨·공조음은 상시 포함되어도 되지만 세션 내내 **동일**해야 한다(6.3(j) "조용한 랩실 ≠ 완전 무음" 주의 승계).
- **센서 앞 1.5m 빈 공간.** ToF 판정(tofJudgeFrame)은 로그하지 않지만 제품과 같은 CPU 부하를 위해 호출한다. 시야에 사람이 들어오면 발광/적분 조건이 바뀔 수 있어 H2 비교가 흐려진다(9.2(e) 시야 오염 주의 준용).
- WiFi·서버 불필요. `secrets.h` 무관.
- 부팅 직후 모드 = **m2**(현행 재현). 모드를 바꾸면 반드시 `[noise][mN] rng=… ok=1` 확인 줄이 뜬다. `ok=0` 이면 그 모드 결과는 무효 — 8절.

---

## 2. 빌드 · 플래시 · 모니터 (분리 실행 — 포트 1개만 점유)

```
cd firmware
~/.platformio/penv/bin/pio run -e mic_noiseprobe -t upload
~/.platformio/penv/bin/pio device monitor -b 115200
```

부팅 로그 기대:
```
[BOOT] ddingdong mic noiseprobe (PoC-45 진단, 키: 0~6 모드 / s 스냅샷)
[BOOT] diag buffers 131072+65536+131072 B PSRAM free=…
[mic][M5a] ring alloc OK: 32 slots x 1024 samples = 65536 bytes (2048ms) | …
[noise][m2] sd_pd req=0 reg=0 gpio=7
[BOOT] micProbeTask started (Core 0, prio 4) — mic_uplink 와 동일 배치
[tof] VL53L5CX ready (8x8, 15Hz, continuous)
[BOOT] tofProbe0/1 started (prio 3) — 활성 = m2 Core 0 @400k (현행 재현)
[noise] ring filled — 's' 스냅샷 / '0'~'6' 모드
```
`[BOOT] tof init 실패` 가 뜨면 모드 전환이 불가하다(마이크 기준선만 측정 가능). 9.1(b) PWREN/LPn 결선을 먼저 본다.

---

## 3. 키와 모드 (재플래시 없음)

| 키 | 모드 | ToF 측정(발광) | 호스트 I2C 읽기 | I2C 클럭 | tofTask 코어 | 바꾼 변수 1개 |
|---|---|---|---|---|---|---|
| `0` | m0 | **OFF** (`stopRanging`, init·전원·버스 결선 유지) | OFF | 400k(무관) | – | 대조군 |
| `1` | m1 | ON | **OFF** (버스 idle) | 400k(무관) | – | m2 대비 읽기 제거 |
| `2` | m2 | ON | ON | 400k | Core 0 | **현행 재현** (= mic_uplink 조건) |
| `3` | m3 | ON | ON | **100k** | Core 0 | m2 대비 클럭만 |
| `4` | m4 | ON | ON | 400k | **Core 1** | m2 대비 코어만 |
| `5` | m5 | ON | ON | 400k | Core 0 | **m2 대비 SD 내부 풀다운만** (2026-09-11 추가) |
| `6` | m6 | **OFF** | OFF | 400k(무관) | – | **m0 대비 SD 내부 풀다운만** (풀다운 부작용 대조군) |
| `s` | – | 2.048초 스냅샷 + 보드 내 분석 (4절 로그 8줄) | | | | |
| `h` | – | 키 요약 1줄 | | | | |

**m5/m6 가 가르는 것 (근거유형 = 논증, 미실측)**: INMP441 은 LSB 출력 직후 SD 를 tri-state 한다
(데이터시트가 SD 에 100kΩ 풀다운을 권장하는 이유). 6.3(n) 의 오류 비트가 워드 **앞쪽**(`7F…` MSB /
100k 의 `C0000000` 상위 2비트)에 몰리는 것은 "Hi-Z → 재구동 직후 샘플되는 비트" 위치와 일치한다.
ESP32-S3 **내부** 풀다운을 SD(GPIO7)에 걸어 **결선 변경 0**으로 그 가설을 가른다.
⚠️ 내부 풀다운은 데이터시트 권장치(100kΩ)와 값이 다르다(S3 내부 ≈ 45kΩ 급, `tof_lineprobe_main.cpp:9`
주석과 같은 근거). 따라서 **효과 없음**이 나와도 "외부 100kΩ 도 무효"로 확대 해석하지 않는다.

**구현 가능 여부 (Step 0 대조 결과)** — 5개 전부 구현. 단 m1 은 전제가 하나 있다:

| 모드 | 판정 | 근거 |
|---|---|---|
| m0 | 가능 | SparkFun `stopRanging()`/`startRanging()` 런타임 호출 = ULD `vl53l5cx_stop_ranging`(레지스터 쓰기 + MCU stop 폴링, `vl53l5cx_api.cpp:551~`) / 재시작 지원 |
| m1 | 가능 **(전제 = 논증)** | ULD `vl53l5cx_check_data_ready` 는 호스트가 0x0 4바이트를 읽어 streamcount 변화만 보는 **호스트측 폴링**이고 ack 쓰기가 없다(`vl53l5cx_api.cpp:591~608`) → 읽지 않아도 센서는 autonomous 로 계속 측정한다고 **논증**. ★ 실측 확인 = 5절 ③ (m1→m2 첫 프레임 `sc a->b` 전진) |
| m2 | 가능 | `initToF()`(tof_common.cpp) 그대로 = 400k / 8x8 / 15Hz / startRanging |
| m3 | 가능 | `Wire.setClock()` = esp32-hal-i2c `i2cSetClock` → `i2c_hal_set_bus_timing` **라이브 재설정**(재init 없음, core 2.0.17 실물 grep) |
| m4 | 가능 | tofTask 를 Core 0/1 에 하나씩 미리 띄우고 활성 코어만 전환(태스크 삭제·재생성 없음) |
| m5·m6 | 가능 | `gpio_pulldown_en/dis`(`driver/gpio.h:267`). ESP32-S3 는 `SOC_GPIO_SUPPORT_RTC_INDEPENDENT (1)`(soc_caps.h) 이라 GPIO7 이 RTC IO 겸용이어도 **디지털 IO_MUX 경로**를 탄다 — 실체는 `gpio_ll_pulldown_en`(hal/esp32s3/…/gpio_ll.h:74) = `REG_SET_BIT(GPIO_PIN_MUX_REG[7], FUN_PD)`. 적용은 `i2s_set_pin` **이후**, 모드 전환마다 재적용하고 **같은 비트를 재독**해 로그로 낸다 |

모드 전환 확인 줄(예): `[noise][m3] rng=1 poll=1 i2c=100k core=0 ok=1 sc=131`
- `rng` 측정 중 / `poll` 호스트 읽기 중 / `i2c` 현재 클럭 / `core` 폴링 코어 / `ok` 전환 API 전부 성공 / `sc` 마지막 성공 프레임 streamcount.
- 이어서 폴링 모드(m2~m5)면 첫 프레임에 `[noise][mN] first frame core=C sc a->b` 1줄.
- 전환 직후 풀다운 상태 1줄: `[noise][m5] sd_pd req=1 reg=1 gpio=7`
  - `req` = 모드 표가 요구한 값 / `reg` = **IO_MUX FUN_PD 재독값**. **`req ≠ reg` 면 그 모드는 실험 무효**
    (풀다운이 실제로 걸리지 않았거나 다른 경로로 갔다는 뜻 — 9절 d 참조).

---

## 4. 스냅샷 로그 8줄 (`s` 1회당, 각 줄 ≤80B — 6.3(j) 시리얼 손상 0 구간)

```
[noise][m2] snap#1 n=32768 peak=32767 rms=512 rms_x=101
[noise][m2] clip=37(+20/-17) burst=9 len_max=6 first=12345
[noise][m2] gap_ms n=8 min=67 mode=67 max=1401 tof_mult=7/8
[noise][m2] raw bad=37 low6nz=37 iso_hi=35 tz=6
[noise][m2] raw ex 40000F40 BFFF0F00 40001000 C0000F80
[noise][m2] tof_rd=31 dt_ms min=0.0 avg=1.2 max=3.1 in=8/9
[noise][m2] slot_dt_us min=63990 max=64010 gaps=0 stk=2296
[noise][m2] sd_pd req=0 reg=0 gpio=7
```

| 줄 | 필드 | 뜻 |
|---|---|---|
| L1 | `peak` | max \|int16\|. **32767 또는 32768 = 클램프 존재** |
| | `rms` / `rms_x` | 전체 RMS / **클램프 샘플 제외** RMS. `rms_x` 가 m0 값(≈95~119)과 같으면 클램프 **외** 구간은 깨끗 |
| L2 | `clip=(+/-)` | 클램프 샘플 수(+32767 / −32768 각각) |
| | `burst` | 클램프 무리 수(간격 < 16 샘플 = 1ms 면 같은 무리). `len_max` 가장 긴 무리 폭(샘플). `first` 첫 클램프 인덱스 |
| L3 | `gap_ms` | 무리 시작→다음 무리 시작 간격(반올림 ms) 최소/최빈/최대. `tof_mult=a/b` = 간격 b개 중 **66.7ms 정수배(±10%)** a개 |
| L4 | `raw bad` | 가드 이전 raw 중 int16 를 벗어나는 샘플 수(= 가드가 잘랐을 수, clip 과 같아야 정상) |
| | `low6nz` | bad 중 **하위 6비트 ≠ 0**. INMP441 정상 데이터는 tz=6(하위 6비트 항상 0, 6.3 실측) → 0 이 아니면 음향이 아니라 **비트 오류** 정황 |
| | `iso_hi` | bad 중 "상위 비트 1개만 뒤집힌 모양"(부호 아래 첫 상이 비트 1개 뒤에 부호 비트 8개 연속). 큰 소리는 이 모양이 잘 안 나온다 |
| | `tz` | 스냅샷 전체 OR 누적의 trailing zeros. 6 이면 정렬 정상(6.3), 그 미만이면 하위 비트가 어딘가에서 섰다 |
| L5 | `raw ex` | bad 샘플 앞 4개 hex(육안) |
| L6 | `tof_rd` | 스냅샷 시점 보유한 ToF 읽기 창 수(최근 64건). `dt_ms` = 각 버스트 시작 시각 → 가장 가까운 ToF **I2C 읽기 창** [isDataReady 직전, getRangingData 직후] 까지 거리. `in=a/b` = b 버스트 중 창 **안**(거리 0) a개 |
| L7 | `slot_dt_us` | 연속 슬롯의 i2s_read 반환 시각 차. **64000 ± 수백 µs 가 정상.** 크게 벗어나면 L6 시각 상관은 신뢰 불가(i2s_read 반환 = DMA 완료 + 큐 대기 상한) |
| | `gaps` / `stk` | i2s_read 실패 누적(0 이어야) / micProbeTask 스택 최저 여유 |
| L8 | `sd_pd req` / `reg` | 이 스냅샷 시점의 SD 풀다운 **요청값 / IO_MUX FUN_PD 재독값**. 측정 조건을 결과와 **같은 스냅샷에** 박아 두는 줄이다. `req ≠ reg` 인 스냅샷은 버린다 |

⚠️ L6 의 시각은 **상한 근사**다: 샘플 시각 = 슬롯 끝 시각 − (1024−k)×62.5µs 로 역산하며, 슬롯 끝 시각엔 태스크 스케줄 지연이 섞인다. `slot_dt_us` 가 64000 근처일 때만 `in`/`dt_ms` 를 근거로 쓴다. 이 한계는 코드로 못 없앤다(DMA 완료 시각을 ISR 없이 얻을 수 없음) — **가능성·근거 보고**: 충분 조건이 아니라 **정합 여부** 확인용.

---

## 5. 절차 (모드별 조용한 스냅샷 3회)

① 부팅 → `ring filled` 확인 → 5초 대기(m2 안정) → `s` ×3 (각 3초 간격) → 8줄×3 기록.
② `0` → 확인 줄 `ok=1` → 5초 대기 → `s` ×3.
③ `1` → 확인 줄 → 5초 대기 → `s` ×3. 그 다음 `2` 를 눌러 **`first frame … sc a->b`** 의 a≠b 를 확인해 기록(a = m1 진입 시 sc, b = 첫 프레임 sc. **전진했으면 m1 동안 센서가 계속 측정했다는 실측 증거** = m1 전제 확인. 같으면 m1 결과를 "센서 정지 상태"로 재해석해야 한다).
④ `2` → `s` ×3 (①과 재현되는지).
⑤ `3` → 확인 줄 `i2c=100k` → 5초 → `s` ×3.
⑥ `4` → 확인 줄 `core=1` + `first frame core=1` → 5초 → `s` ×3.
⑦ 마지막에 `2` → `s` ×1 (세션 중 환경 드리프트 확인 — ①·④와 같은 자릿수여야 한다).

**⑧ SD 풀다운 판별 (m5/m6, 2026-09-11 추가)** — 같은 자리·같은 결선·조용한 환경에서 **교대**로 찍는다
(세션 중 환경 드리프트를 상쇄하려는 것이라 "m2 3회 → m5 3회" 로 몰아 찍지 않는다):

```
2 → s×3   5 → s×3   2 → s×3   5 → s×3   0 → s×3   6 → s×3
```

- 각 모드 진입 직후 `sd_pd req= reg=` 줄을 먼저 확인한다. **m5·m6 = `reg=1`, 나머지 = `reg=0`** 이 아니면
  그 구간은 실험 무효이므로 찍지 말고 9절 d 로 간다.
- 각 모드 진입 후 5초 대기는 ①~⑦과 동일.

**⑧ 기록 표** (교대 순서 그대로 1행씩):

| 순서 | 모드 | sd_pd req/reg | # | clip(+/−) | burst | peak | rms | rms_x | bad | low6nz | iso_hi | tz |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | m2 | / | 1 | | | | | | | | | |
| 2 | m5 | / | 1 | | | | | | | | | |
| … | | | | | | | | | | | | |

**기록 표 양식** (스냅샷 1회 = 1행. 8줄에서 옮겨 적는다):

| 모드 | # | sd_pd req/reg | peak | rms | rms_x | clip(+/−) | burst | len_max | gap min/mode/max | tof_mult | bad | low6nz | iso_hi | tz | dt_ms min/avg/max | in | slot_dt min/max | gaps |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| m2 | 1 | / | | | | | | | | | | | | | | | | |
| … | | | | | | | | | | | | | | | | | | |

m1 전제 확인: `sc a->b` = ____ → ____ (전진 O/X)

---

## 6. ★ 판정표 (가설 → 기대 패턴). **지지 / 기각까지만** — 해결책은 적지 않는다

전제: m0 3회가 전부 `clip=0`, `rms_x≈rms≈m0 기준선`(A/B 실측 95~96 자릿수)이어야 이하가 성립한다. m0 에서도 클립이 나오면 원인은 ToF 측정 밖(§9-a 정지·보고).

| 관측 패턴 | 읽는 법 |
|---|---|
| m0·m1 깨끗(clip=0) **&** m2 오염 | 클립은 **호스트 I2C 트래픽이 있을 때만** 발생 → H1 또는 H3 지지, H2 기각 |
| m1 오염 | 호스트가 버스를 안 만지는데도 발생 → **H2(센서 동작 전원) 지지**, H1·H3 단독으로는 설명 불가. (m1 전제 `sc` 전진 확인 필수) |
| m3 `clip`·`burst` 가 m2 대비 **뚜렷이 감소**(같은 자릿수 아님) | 클립 수가 SCL 엣지 수/속도에 비례 → **H1 지지**. (100k 에서 트랜잭션은 길어지고 엣지 수는 같으므로, 감소는 "엣지 기울기·속도" 의존을 뜻한다 — 논증) |
| m3 ≈ m2 | 클럭 무관 → H1 의 "엣지 속도" 변형은 약해지나 H1 자체 기각은 아님(엣지 **수**는 같다) |
| m4 ≈ m2 | 코어를 바꿔도 같음 → **H3 기각** |
| m4 뚜렷이 감소/소멸 | Core 0 동거가 조건 → **H3 지지** (H1·H2 는 코어와 무관해야 하므로 약화) |
| `gap_ms` 최빈/다수가 66.7ms 배수(`tof_mult` 비율 높음) | 클립이 **ToF 프레임 주기에 동기** → I2C 읽기(폴링 성공 프레임) 또는 발광 주기와 결합. m1/m2 비교로 어느 쪽인지 가른다(m1 에서도 배수면 발광 동기 = H2 쪽) |
| L6 `in` 비율 높음 + `slot_dt_us` 정상 | 클립이 I2C 읽기 창 **안**에 몰림 → H1 정합(H3 도 배제는 안 됨) |
| L4 `low6nz > 0` 또는 `iso_hi ≈ bad` | raw 가 **음향 파형이 아니라 비트 오류** 모양 → 전기적 유입(H1) 정합. 진짜 큰 소리(포화 음향)라면 low6nz=0 · iso_hi≈0 · 파형 연속 |
| L4 `tz < 6` | 하위 비트까지 섰다 → I2S 라인 자체에 비트 오류 = H1 정합 |
| `rms_x ≈ m0 rms` | 클램프 샘플만 제외하면 배경은 정상 → 잡음이 **연속 노이즈 플로어 상승이 아니라 이산 사건** |

⚠️ 한 행만으로 결론내지 말 것. 최소 m0·m1·m2 세 모드의 교차로 H2 를 먼저 가르고, m3·m4 로 H1/H3 을 나눈다. 판정 결과는 decisions.md 에 "지지/기각 + 어느 행이 근거" 형식으로 등재하고 **해결책은 별도 결정**으로 남긴다.

### 6-1. SD 풀다운 해석 가이드 (m5/m6 — **해석**까지이고 제품 결정이 아니다)

전제: ⑧의 **교대 m2** 가 그 세션에서 실제로 `clip ≥ 1` 을 재현해야 한다. 대조군이 재현되지 않은
세션의 m5 결과는 아무것도 뜻하지 않는다.

| 관측 | 읽는 법 |
|---|---|
| 교대 m2 가 `clip ≥ 1` 재현 **&** m5 전 스냅샷 `clip = 0` | 풀다운이 증상을 없앰 → **SD tri-state 부유 가설 정합**. 제품 반영 여부(6.3(n) ①/②/③)는 **별도 판단** |
| m5 가 교대 m2 와 **같은 범위** | 풀다운 무효 → 부유 라인 가설 약화. 6.3(n) ①(펌웨어 필터) 쪽 검토 |
| m5 가 **감소했으나 0 아님** | 부분 기여 → 풀다운 + ① 병행 검토. 수치만 기록하고 결론은 사용자 |
| 교대 m2 가 이 세션에서 `clip = 0` | **대조군 재현 실패 → 판정 보류.** m5 결과를 근거로 쓰지 않는다 |
| m6 vs m0 | 풀다운 **자체의 부작용** 대조. 차이가 나면 풀다운이 신호 자체를 건드린다는 뜻 — 수치는 기록하고 판정은 사용자 |

⚠️ 내부 풀다운(≈45kΩ)은 데이터시트 권장 외부 100kΩ 과 값이 다르다. "효과 없음"이 나와도 외부 저항까지 무효라고 확대하지 않는다.

---

## 7. 호스트 단위 검증 (하드웨어 없이 언제든 재실행)

```
c++ -std=c++17 -Wall -I firmware/include -o /tmp/nst firmware/tools/noise_stats_test.cpp && /tmp/nst
```
`firmware/include/noise_stats.h`(보드가 쓰는 **같은 파일**)를 합성 32768 샘플 스냅샷으로 검산한다 — 클램프 버스트를 알려진 위치·길이로 심고 개수/간격/최빈/배수/rms/제외-rms/비트 패턴/시각 상관을 단언(2026-09-11 61 checks OK).

negative control(각각 **반드시 실패**해야 통과). 실행은 헤더를 **사본에 복사해 변형**하고 `-I <사본 dir>` 로 컴파일한다(원본 무변경 = `git status` 로 확인). 4단계 = ① 변형이 적용됐는가(`diff` 1줄 이상) ② baseline 통과 ③ 변형 실패 ④ 원본 무변경.

| # | 변형 (`noise_stats.h` 1건 치환) | 2026-09-11 결과 | 검출 위치 |
|---|---|---|---|
| NC-1a | `st.gap_n = recorded - 1` → `recorded` (간격 개수 off-by-one) | **검출** | T1 `gap_n == 1` |
| NC-1b | 버스트 경계 `>= NOISE_BURST_GAP_SAMPLES` → `>` | **검출** | T1 거리 정확히 16 → `burst_n == 2` |
| NC-1c | 간격 샘플 수 `+1`(1 샘플 = 62.5µs) | **미검출 — 도달 불가(관측 해상도)** | 출력이 반올림 ms 라 62.5µs 편향은 어떤 단언으로도 관측 불가. 같은 변형을 `+16`(=1ms, 표시 해상도) 로 두면 T1 `gap_min_ms == 1` 에서 **검출** — 분석기의 판별 한계가 1ms 라는 사실 자체가 결과 |
| NC-2 | 클램프 판정 `== 32767 \|\| == -32768` → `>= 32766 \|\| <= -32767` | **검출** | T1 `clip_n == 2`(32766/−32767 을 세면 4) |
| NC-3 | 제외 rms 분모 `n_ok` → `n` | **1차 미검출 → 테스트 보강 후 검출** | 1차: T2 는 클램프 26/32768(0.08%)이라 ±1 허용 안에 숨었다(판별력 부족 = 카테고리 20 「미검출의 판정」의 (2) 경로). T3b(50% 클램프, `rms_excl == 1000` 정확 일치) 추가 → 분모 오류가 √2 배로 드러나 **검출** |
| NC-4 | 배수 허용폭 10% → 100% (`/100u` → `/10u`) | **검출** | T5 `!noiseIsPeriodMultiple(100, 667, 10)` |

### 7-1. 모드 불변식 (`noise_modes.h` — 「한 번에 한 변수만」 자체를 고정)

```
c++ -std=c++17 -Wall -I firmware/include -o /tmp/nmt firmware/tools/noise_modes_test.cpp && /tmp/nmt
```
보드가 쓰는 **같은 파일**을 include 해 모드 → 구성 매핑을 전 필드 비교한다(2026-09-11 **46 checks OK**).

- **I1** m5 는 m2 와, m6 는 m0 와 **정확히 `sd_pd` 한 필드만** 다르다(ranging/poll/i2c_hz/core/sd_pd 전부 비교).
- **I2** 기존 m0~m4 의 `sd_pd` 는 전부 OFF (켜지면 6.3(n) 과거 실측과 비교 불가).

negative control(각각 **반드시 실패**). 4단계 = ① 앵커 정확히 1건 ② 변형본 ≠ 원본 ③ 디스크 재독 ④ 복원 후 `git diff --exit-code`.

| # | 변형 (`noise_modes.h` 1건 치환) | 2026-09-11 결과 | 검출 위치 |
|---|---|---|---|
| NC-M1 | m5 의 `c.sd_pd = true` → `false` | **검출** | `diffCount(cfg(5), cfg(2)) == 1` (I1). 실험군이 대조군과 같아져 실험이 조용히 무효가 되는 경로 — "m5 가 존재한다"만 보는 단언이면 못 잡는다 |
| NC-M2 | m5 에 `c.i2c_hz = slow_hz` 추가 (클럭 오염) | **검출** | 같은 I1 단언. 두 변수가 동시에 바뀌어 m3 와 혼동되는 경로 — 일부 필드만 비교하면 못 잡는다 |
| NC-M3 | 기존 m2 에 `c.sd_pd = true` 추가 | **검출** | `cfg(m).sd_pd == false` (I2) |

⚠️ **한계**: 이 테스트가 고정하는 것은 **모드 표**까지다. 레지스터가 실제로 섰는지(`gpio_pulldown_en` 의
IO_MUX 반영)는 호스트에서 재현할 수 없다 — 그래서 그 검증을 **런타임 로그 `sd_pd req=/reg=`** 로 옮겼다.

---

## 8. 실패 시 로그 대응표

| 로그 | 뜻 / 조치 |
|---|---|
| `[BOOT] PSRAM 진단 버퍼 확보 실패` | 328KB PSRAM 확보 실패. 스냅샷 불가. PSRAM 8MB 보드에서 정상 부팅이면 나오지 않는다 |
| `[BOOT] tof init 실패` | 9.1(b) PWREN/LPn 결선 확인. 모드 전환 불가(마이크 기준선만) |
| `[noise][mN] … ok=0` | stop/start/setClock 중 하나 실패. 그 모드 결과 무효. `2` 로 복귀 후 재시도. 반복되면 재부팅 |
| `first frame` 줄이 m2~m4 전환 후 2초 넘게 안 뜸 | 폴링 태스크가 프레임을 못 받음(isDataReady 계속 false). m0→m2 직후라면 startRanging 재시작 지연 가능 — 5초 더 대기 |
| `slot_dt_us` 가 64000 에서 수 ms 이상 이탈 | 태스크 지연. L6 시각 상관 무효(L1~L5 는 유효) |
| `gaps > 0` | i2s_read 실패 발생 = DMA 오버런 성격. 해당 스냅샷은 H3 판정에 쓰지 말 것(mic_uplink 실측은 gaps=0) |
| `[noise] 이전 스냅샷 처리 중` | `s` 연타. 8줄 출력 후 다시 |
| `sd_pd req=1 reg=0` (또는 `req=0 reg=1`) | 풀다운 요청이 레지스터에 반영되지 않음. **그 모드 결과 전부 무효.** 해당 모드 재진입 → 그래도 같으면 9절 d |

---

## 9. 정지 트리거 (보고 후 사용자 판단)

- a) **m0 에서도 clip > 0** — "원인 = ToF 측정" 실측 전제와 충돌. 배경(마이크 자체·전원·다른 소음원)을 먼저 의심해야 하며 이 Runbook 의 판정표는 무효.
- b) `ok=0` 이 재부팅 후에도 반복 — 모드 전환 수단 자체가 신뢰 불가.
- c) m1→m2 `sc a->b` 에서 a == b — m1 동안 센서가 측정을 멈췄다는 뜻이라 m1 = m0 과 같은 조건이 되어 H2 격리 불가(설계 전제 붕괴 → m1 재설계 필요, 이 문서 범위 밖).
- d) **`sd_pd req ≠ reg` 가 재진입 후에도 반복** — 풀다운이 IO_MUX 에 반영되지 않는다(RTC 경로 등). m5/m6 판별 수단 자체가 신뢰 불가 → 보고 후 사용자 판단.
- e) **교대 m2 가 `clip = 0`** — 대조군 재현 실패. 6-1 판정 보류(m5 수치는 기록만).

---

## 10. 이 하네스가 바꾸지 않는 것

- `mic_common.*` / `tof_common.*` / `mic_uplink_main.cpp` / `uplink_common.*` / 프로즌 env 전부: **0줄**. `platformio.ini` 는 `[env:mic_noiseprobe]` 블록 append 만.
- 판정 상수(`TOF_*`, `MIC_RAW_TO_INT16_SHIFT` 등) 0개 변경. 신규 lib 0.
- 결선. 해결책.
- **풀다운의 제품 경로 반영**: m5/m6 는 `mic_noiseprobe_main.cpp` 안에서만 풀다운을 건다. `mic_common.cpp` 의 `initMicI2S()` 는 **0줄 변경**이며, 제품 펌웨어(`env:mic_uplink`)의 SD 핀 설정은 그대로다. 반영 여부는 실측 확인 후 별도 PR·사용자 판단.
