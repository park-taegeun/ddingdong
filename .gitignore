# === PlatformIO ===
.pio/
.vscode/

# === PlatformIO compile_commands.json (절대경로 포함, push 금지) ===
# pio run -t compiledb 부산물. clangd가 자동 인식하므로 commit 불필요.
# leading slash 없는 패턴은 모든 하위 디렉토리 매칭 (gitignore PATTERN FORMAT)
compile_commands.json

# === clangd 인덱스 캐시 (IDE 전용, push 금지) ===
.cache/
.clangd/.cache/

# === Python ===
venv/
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# === SQLite DB (서버 로컬 데이터, push 금지) ===
*.db
*.db-journal

# === Node ===
node_modules/
dist/
build/

# === Playwright MCP E2E 부산물 (스냅샷/콘솔 로그, push 금지) ===
.playwright-mcp/

# === macOS ===
.DS_Store

# === 환경변수 (실 파일은 절대 push 금지) ===
.env
.env.local

# === 펌웨어 자격증명 (실 파일은 절대 push 금지, template만 commit) ===
firmware/include/secrets.h

# === ML 데이터/모델 placeholder (8주차 진입 시 정책 재정의) ===
ml/data/
ml/models/
*.tflite
*.h5

# === ML 오디오 파일 (git 커밋 0 — 코드/config/manifest 스키마만 push) ===
*.wav
*.flac
*.mp3
*.m4a
*.ogg
# 파이프라인 산출 스테이지 (실 데이터는 repo 밖이지만 방어적 차단)
ml/**/02_preprocessed/
ml/**/03_augmented/
ml/**/05_final_dataset/
ml/**/manifests/
