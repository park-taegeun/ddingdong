# 띵동 Phase 2-1 백엔드 의존성 (PyPI 최신 안정 버전, 2026-06-14 확인)
# 카테고리 6: SQLite + Flask-SQLAlchemy / 카테고리 27.5: 버전 가정 검증 강제
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
python-dotenv==1.2.2
# 카테고리 6.2: /detect 가 server/inference/audio_decode.py(sibling, frozen)를 import 호출
# → 그 모듈의 유일한 의존성인 numpy 를 라이브 서버 venv 에도 설치 (TF 는 여기서 끌려오지 않음).
numpy==2.5.1
